export interface ChildUser {
    id: number;
    firstname: string;
    lastname: string;
    dateOfBirth: string; // or Date type if preferred
    phoneNumber: string;
    email: string;
  }

  